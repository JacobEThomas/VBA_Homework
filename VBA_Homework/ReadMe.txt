This VBA code finds the total stock volume per year of stocks.

Data must be formatted identically to the csv in the resources directory to run.

Images are saved in the images directory of the code open in Visual Studio, as well as images of code implemented on the sample dataset.